# TB FHIR Phylogeny Analysis Pipeline (Local)

A Nextflow pipeline for local Mycobacterium tuberculosis phylogenetic analysis from FHIR Genomics bundles. Generating SNP distance matrices, phylogenetic trees, and transmission network visualizations. [Full documentation](https://federated-tb-docs.readthedocs.io)

## Features

- **FHIR Genomics Data Gateway:** Fetch patient FHIR genomic bundles directly from a FHIR server using OAuth2 authentication.
- **Local FHIR Data Input:** Support for local processing of FHIR Bundles (offline mode).
- **Phylogenetic Analysis:** Generates SNP distance matrices and phylogenetic trees.
- **Transmission Network:** Visualization and statistical plots (histogram, heatmap, violin plot).
- **Federated Analysis:** Generates pooled coefficient input for phylogenetic federated analytics (global model).
- **Clinical Metadata Integration:** Extracts metadata (location, lineage, patient ID) from FHIR resources.

## Installation

```bash
git clone https://github.com/oucru-id/tb-phylo-analysis-local.git
cd local_phylo_analysis
```

## Requirements

- [Nextflow](https://www.nextflow.io/)
- Python 3.8+

Install Python dependencies:
```bash
pip install biopython pandas networkx pyvis matplotlib seaborn numpy requests
```

## Directory Structure

```
local_phylo_analysis/
├── main.nf                        # Main workflow
├── nextflow.config                # Configuration and parameters
├── workflows/
│   ├── phylo.nf                   # Phylogenetic analysis workflow
│   ├── visualization.nf           # Visualization workflow
│   └── utils.nf                   # Utility functions
├── scripts/
│   ├── fhir_phylo.py              # Core SNP distance + tree builder
│   ├── fetch_fhir_data.py         # FHIR server data fetcher
│   ├── get_access_token.py        # Standalone OAuth2 token generator
│   ├── visualize_results.py       # Network and tree visualizations
│   └── get_versions.py            # Version collection
└── data/
    ├── H37Rv.fasta                # Reference genome
    ├── anchor/                    # Anchor FHIR JSON samples
    ├── JSON/                      # Local input FHIR JSON files
    ├── access_token.json          # OAuth2 token (generated by get_access_token.py)
    └── input_sso.json             # SSO credentials for token generation
```

## Input Data

- FHIR Bundle Genomics JSON files in `data/JSON/` (local mode)
- FHIR Bundle Genomics from FHIR server (FHIR server mode)
- Reference genome FASTA: `data/H37Rv.fasta`
- Anchor samples: `data/anchor/*.json`

## Usage

### Local Mode (default)

Place FHIR JSON files in `data/JSON/` and run:

```bash
nextflow run main.nf
```

### FHIR Server Mode

**Step 1: Get Access Token**

Fill in `data/input_sso.json` with your SSO credentials, then run:
```bash
python3 scripts/get_access_token.py
```
This generates `data/access_token.json`.

**Step 2: Configure**

In `nextflow.config`, set:
```groovy
use_fhir_server    = true
fhir_server_url    = "https://<BASE_URL>/fhir"
fetch_since        = ""  // Optional: "YYYY-MM-DD" to fetch only recent data
```

**Step 3: Run**
```bash
nextflow run main.nf
```

### Key Parameters (`nextflow.config`)

| Parameter | Default | Description |
|-----------|---------|-------------|
| `use_fhir_server` | `false` | `true` to fetch from FHIR server, `false` for local files |
| `fhir_dir` | `data/JSON` | Local directory of FHIR JSON files |
| `fhir_server_url` | `""` | FHIR server base URL |
| `access_token_file` | `data/access_token.json` | Path to OAuth2 token file |
| `fetch_since` | `""` | Fetch only records updated after this date (`YYYY-MM-DD`) |
| `reference` | `data/H37Rv.fasta` | Reference genome |
| `snp_threshold` | `12` | SNP distance threshold for transmission network edges |

## Output Structure

```
results/
├── phylo/
│   ├── distance_matrix.tsv        # Pairwise SNP distance matrix
│   ├── phylo_tree.nwk             # Newick phylogenetic tree
│   ├── metadata.tsv               # Sample metadata (lineage, location, patient ID)
├── fetched_data/                  # FHIR JSON files fetched from server
├── visualization/
│   ├── transmission_network.html  # SNP transmission network
│   ├── stats_histogram.png        # SNP distance distribution
│   ├── stats_heatmap.png          # SNP distance heatmap
│   ├── stats_violin.png           # SNP distance violin plot
│   └── phylo_tree_rectangular.png # Rectangular phylogenetic tree
└── runningstat/
    ├── execution.html
    ├── timeline.html
    └── dag.html
```

## Support

[GitHub Issues](https://github.com/oucru-id/tb-phylo-analysis-local/issues)
